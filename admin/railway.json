{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build",
    "outputDirectory": "dist"
  },
  "deploy": {
    "startCommand": "npx serve dist -l 8080",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
